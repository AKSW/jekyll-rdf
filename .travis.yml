language: ruby
rvm:
- 2.2.4
- 2.3.0
- ruby-head
matrix:
  allow-failures:
    - rvm: ruby-head
install: bundle install --jobs=3 --retry=3
script: 
- cd test-site
- bundle install --jobs=3 --retry=3
- bundle exec jekyll build
